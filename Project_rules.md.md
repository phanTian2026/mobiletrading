# Project Rules — 任务执行和状态管理

## 1. 任务状态管理

每个任务将根据状态进行跟踪，并在每个阶段自动更新状态：

- **待执行**：任务尚未开始。
- **执行中**：任务正在执行。
- **已完成**：任务执行成功，生成结果。
- **待修改**：任务执行失败，需要改进或修改。

## 2. 任务规则加载和执行

### 2.1 因子研究任务
#### 加载规则：
- [factor-library.md](../rules_archive/factor-library.md)
- [safe-math.md](../rules_archive/safe-math.md)

#### 执行步骤：
1. 加载因子库规则
2. 执行因子研究，生成10个初步因子
3. 生成因子IC分析结果
4. 提供反馈：
   - 如果因子有效，标记为“已完成”。
   - 如果因子无效，标记为“待修改”，并给出优化建议。

#### 状态：
- [ ] **待执行**：待加载规则并执行因子研究
- [ ] **执行中**：因子研究执行中
- [ ] **已完成**：因子研究完成，生成因子和分析结果
- [ ] **待修改**：因子研究失败，存在优化空间

---

### 2.2 回测验证任务
#### 加载规则：
- [future-function-prevention.md](../rules_archive/future-function-prevention.md)
- [backtest-optimization.md](../rules_archive/backtest-optimization.md)

#### 执行步骤：
1. 加载回测验证规则
2. 执行回测，验证策略表现
3. 检查回测日志，验证是否存在未来函数
4. 提供反馈：
   - 如果回测通过，标记为“已完成”。
   - 如果回测失败，标记为“待修改”，并提供策略优化建议。

#### 状态：
- [ ] **待执行**：待加载回测验证规则并执行回测
- [ ] **执行中**：回测正在执行
- [ ] **已完成**：回测成功，策略有效
- [ ] **待修改**：回测失败，回测失败原因：存在未来函数

---

### 2.3 策略审稿任务
#### 加载规则：
- [strategy-evaluation.md](../rules_archive/strategy-evaluation.md)
- [risk-assessment.md](../rules_archive/risk-assessment.md)

#### 执行步骤：
1. 加载策略评估规则
2. 审查策略的回测结果，确保其有效性
3. 执行风险评估，检查策略的最大回撤、夏普比率等
4. 提供反馈：
   - 如果策略有效，标记为“已完成”。
   - 如果策略无效，标记为“待修改”，并提供优化建议。

#### 状态：
- [ ] **待执行**：待加载策略评估规则并审查策略
- [ ] **执行中**：策略审查执行中
- [ ] **已完成**：策略审查完成，策略有效
- [ ] **待修改**：策略审查失败，存在改进空间

---

## 3. 任务执行反馈与建议

每个任务完成后，都会自动生成反馈报告，内容包括：

- **任务执行状态**：如“已完成”、“执行中”或“待修改”。
- **任务结果**：例如“因子有效”，“回测通过”等。
- **改进建议**：如果任务未通过，给出建议修改的方向（例如：“因子无效，调整因子计算方式”，“策略存在未来函数，重写数据输入逻辑”等）。

## 4. 任务优先级和调度

为确保任务执行的高效性和优先级合理分配，每个任务将按优先级分配执行顺序：

- **高优先级**：回测验证任务、策略审稿任务
- **中优先级**：因子研究任务、数据清洗任务
- **低优先级**：因子优化、参数调优

## 5. 动态任务调度

任务调度器将根据用户输入和任务类型动态调度任务，并根据规则自动调整执行路径。例如：

- 如果用户输入“因子研究”，系统将加载 **因子研究规则**，并在执行后自动返回结果。
- 如果用户输入“回测验证”，系统将加载 **回测规则**，并执行回测、策略验证等步骤。

## 6. 任务状态跟踪

### 任务列表：

1. 因子研究任务
   - **状态**：待执行
   - **规则**：[factor-library.md](../rules_archive/factor-library.md)
2. 回测验证任务
   - **状态**：待执行
   - **规则**：[backtest-optimization.md](../rules_archive/backtest-optimization.md)
3. 策略审稿任务
   - **状态**：待执行
   - **规则**：[strategy-evaluation.md](../rules_archive/strategy-evaluation.md)

---

### 示例任务执行过程：

#### 因子研究任务：
```text id="fj30vm"
任务：因子研究

状态：执行中
规则：加载因子库规则 [factor-library.md]
反馈：因子有效，已生成10个有效因子，待进一步回测。

状态更新：
- [ ] 因子研究：已完成